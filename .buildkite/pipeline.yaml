steps:
  - label: "🔍 Lint & Format"
    key: "lint"
    plugins:
      - docker-compose#v5.12.1:
          run: lint
          config: compose.ci.yml
      - docker-compose#v5.12.1:
          run: fmt
          config: compose.ci.yml

  - label: "🧪 Test"
    key: "test"
    plugins:
      - docker-compose#v5.12.1:
          run: test
          config: compose.ci.yml
